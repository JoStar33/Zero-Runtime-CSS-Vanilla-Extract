import { style } from '@vanilla-extract/css';

const dragWrapperStyle = {
  main: style({
    display: 'flex',
    flexDirection: 'column',
    gap: '20px',
  }),
};

export default dragWrapperStyle;
